{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "HotGo Server (Debug)",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/server",
      "cwd": "${workspaceFolder}/server",
      "console": "integratedTerminal",
      "env": {
        "GF_ENV": "develop"
      }
    }
  ]
}
