CREATE TABLE `hg_addon_lazysheep_tggo_bot` (
  `id` INTEGER NOT NULL,
  `bot_key` TEXT NOT NULL DEFAULT '',
  `role` TEXT NOT NULL DEFAULT 'user',
  `member_id` INTEGER DEFAULT 0,
  `token` TEXT NOT NULL DEFAULT '',
  `bot_name` TEXT DEFAULT '',
  `username` TEXT DEFAULT '',
  `webhook_secret` TEXT DEFAULT '',
  `webhook_path` TEXT DEFAULT '',
  `enabled` INTEGER DEFAULT 1,
  `auto_pull` INTEGER DEFAULT 0,
  `auto_forward` INTEGER DEFAULT 0,
  `review_enabled` INTEGER DEFAULT 1,
  `allow_verify` INTEGER DEFAULT 1,
  `allow_location` INTEGER DEFAULT 1,
  `member_verify` INTEGER DEFAULT 0,
  `member_points` INTEGER DEFAULT 0,
  `sign_follow` INTEGER DEFAULT 0,
  `sign_channels` json DEFAULT NULL,
  `review_text` TEXT DEFAULT '',
  `publish_text` TEXT DEFAULT '',
  `sort` INTEGER DEFAULT 0,
  `status` INTEGER DEFAULT 1,
  `created_by` INTEGER DEFAULT 0,
  `updated_by` INTEGER DEFAULT 0,
  `created_at` datetime DEFAULT NULL,
  `updated_at` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
);

CREATE TABLE `hg_addon_lazysheep_tggo_binding` (
  `id` INTEGER NOT NULL,
  `binding_key` TEXT NOT NULL DEFAULT '',
  `bot_id` INTEGER NOT NULL,
  `bot_key` TEXT NOT NULL DEFAULT '',
  `source_url` TEXT NOT NULL DEFAULT '',
  `source_token` TEXT DEFAULT '',
  `source_room_id` INTEGER DEFAULT 0,
  `source_pair_id` TEXT DEFAULT '',
  `review_chat_id` INTEGER DEFAULT 0,
  `publish_chat_id` INTEGER DEFAULT 0,
  `auto_push` INTEGER DEFAULT 0,
  `review_enabled` INTEGER DEFAULT 1,
  `publish_enabled` INTEGER DEFAULT 1,
  `verify_enabled` INTEGER DEFAULT 1,
  `location_enabled` INTEGER DEFAULT 1,
  `plugin_settings` TEXT DEFAULT '',
  `last_pull_id` INTEGER DEFAULT 0,
  `last_cursor` TEXT DEFAULT '',
  `status` INTEGER DEFAULT 1,
  `created_by` INTEGER DEFAULT 0,
  `updated_by` INTEGER DEFAULT 0,
  `created_at` datetime DEFAULT NULL,
  `updated_at` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
);

CREATE TABLE `hg_addon_lazysheep_tggo_user` (
  `id` INTEGER NOT NULL,
  `telegram_id` INTEGER NOT NULL,
  `member_id` INTEGER DEFAULT 0,
  `username` TEXT DEFAULT '',
  `first_name` TEXT DEFAULT '',
  `last_name` TEXT DEFAULT '',
  `language_code` TEXT DEFAULT '',
  `is_bot` INTEGER DEFAULT 0,
  `member_level` INTEGER DEFAULT 0,
  `points` decimal(10,2) DEFAULT 0.00,
  `last_active_at` datetime DEFAULT NULL,
  `status` INTEGER DEFAULT 1,
  `created_at` datetime DEFAULT NULL,
  `updated_at` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
);

CREATE TABLE `hg_addon_lazysheep_tggo_note` (
  `id` INTEGER NOT NULL,
  `bot_id` INTEGER DEFAULT 0,
  `binding_id` INTEGER DEFAULT 0,
  `content_id` INTEGER DEFAULT 0,
  `up_id` INTEGER DEFAULT 0,
  `pair_id` TEXT DEFAULT '',
  `receiver_room_id` INTEGER DEFAULT 0,
  `room_name` TEXT DEFAULT '',
  `sender` TEXT DEFAULT '',
  `sender_dno` TEXT DEFAULT '',
  `sender_user` json DEFAULT NULL,
  `raw_payload` json DEFAULT NULL,
  `note_payload` json DEFAULT NULL,
  `message_type` TEXT DEFAULT 'MESSAGE_TYPE_NOTES',
  `code` TEXT DEFAULT '',
  `title` TEXT DEFAULT '',
  `text_content` TEXT DEFAULT '',
  `workflow_status` INTEGER DEFAULT 1,
  `review_message_id` INTEGER DEFAULT 0,
  `publish_message_id` INTEGER DEFAULT 0,
  `approved_by` INTEGER DEFAULT 0,
  `published_by` INTEGER DEFAULT 0,
  `approved_at` datetime DEFAULT NULL,
  `published_at` datetime DEFAULT NULL,
  `last_error` TEXT DEFAULT '',
  `sort` INTEGER DEFAULT 0,
  `status` INTEGER DEFAULT 1,
  `created_at` datetime DEFAULT NULL,
  `updated_at` datetime DEFAULT NULL,
  `deleted_at` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
);

CREATE TABLE `hg_addon_lazysheep_tggo_note_item` (
  `id` INTEGER NOT NULL,
  `note_id` INTEGER NOT NULL,
  `item_index` INTEGER DEFAULT 0,
  `item_type` TEXT NOT NULL DEFAULT '',
  `title` TEXT DEFAULT '',
  `sub_title` TEXT DEFAULT '',
  `content` TEXT DEFAULT '',
  `duration` INTEGER DEFAULT 0,
  `aspect_ratio` decimal(10,4) DEFAULT 0.0000,
  `verify_video` INTEGER DEFAULT 0,
  `attachment_id` INTEGER DEFAULT 0,
  `preview_url` TEXT DEFAULT '',
  `local_path` TEXT DEFAULT '',
  `tg_file_id` TEXT DEFAULT '',
  `status` INTEGER DEFAULT 1,
  `created_at` datetime DEFAULT NULL,
  `updated_at` datetime DEFAULT NULL,
  `deleted_at` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
);
